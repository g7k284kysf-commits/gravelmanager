# ADR 001: FastAPI backend

Status: accepted. FastAPI remains the typed REST boundary because existing Sprint 1/2 behavior, Pydantic validation and OpenAPI are established. Domain orchestration stays in services rather than route handlers.
