# ADR 004: JWT and membership authorization

Status: accepted. Existing bcrypt passwords and bearer JWT authentication are preserved. Authorization additionally resolves an active tenant membership and scopes athlete-owned queries. Organization switching is deferred.
