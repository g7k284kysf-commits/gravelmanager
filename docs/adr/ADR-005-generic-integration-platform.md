# ADR 005: Generic integration platform

Status: accepted. Capability-declaring adapters emit provider-neutral records through a duplicate-safe registry. Provider code cannot leak into product services. Non-live adapters must return unsupported operations and accurate metadata.
