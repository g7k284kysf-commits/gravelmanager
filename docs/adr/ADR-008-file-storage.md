# ADR 008: Object-storage interface

Status: accepted. Bytes live behind a streaming interface; local development uses a generated-key volume. PostgreSQL stores metadata. The same interface permits S3-compatible storage without changing import orchestration.
